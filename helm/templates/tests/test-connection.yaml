apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "twitter_trend.fullname" . }}-test-connection"
  labels:
    {{- include "twitter_trend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "twitter_trend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
