pipeline {
    agent {
        node {
            label 'maven-jenkins-agent'
        }
    }
    environment {
        PATH = "/opt/apache-maven-3.9.9/bin:$PATH"

    }
    stages {
        stage('clone the code') {
            steps {
                sh 'mvn clean deploy'
            }
        }
    }
}